<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.github.programmerrabbit.dao.UserDao">

    <insert id="insert" parameterType="com.github.programmerrabbit.entity.User">
        INSERT INTO User
        (
            username,
            password
        )
        VALUES
        (
            #{entity.username},
            #{entity.password}
        )
    </insert>

    <update id="updateById">
        UPDATE User
        SET
            username = #{entity.username},
            password = #{entity.password}
        WHERE
        id = #{id}
    </update>
</mapper>